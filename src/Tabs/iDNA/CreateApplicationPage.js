import React from 'react'
import {View, Text, FlatList, ActivityIndicator, TouchableOpacity, TouchableHighlight} from 'react-native'

import { List, ListItem, SearchBar } from "react-native-elements";

import FastImage from 'react-native-fast-image'



export default class CreateApplicationPage extends React.Component{
    render(){
        return(
            <View><Text>CreateApplicationPage</Text></View>
        )
    }
}